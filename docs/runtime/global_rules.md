# Global Rules

这些规则适用于所有 Agent。

1. 不允许把所有原始数据一次性丢给 LLM 直接生成完整报告。
2. 必须保留来源 URL 和证据引用。
3. 每个阶段都必须保存自己的输出产物。
4. 阶段之间优先使用结构化 JSON 传递。
5. 不允许编造来源事实。
6. 如果来源信息不足，要标记为不确定，而不是补写不存在的细节。
7. 最终报告里的重要判断必须能追溯到结构化记录。
8. prompt、skill、hook、final quality hook 和输出产物都要可追踪。
